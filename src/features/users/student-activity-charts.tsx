"use client";

import { Bar, BarChart, CartesianGrid, Cell, Legend, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";

export function StudentActivityCharts({ byBank, correct, incorrect }: { readonly byBank: readonly { readonly name: string; readonly answered: number }[]; readonly correct: number; readonly incorrect: number }) {
  const answerResult = [{ name: "Correct", value: correct, color: "#3f6577" }, { name: "Incorrect", value: incorrect, color: "#e59a88" }];
  return <div className="grid gap-6 xl:grid-cols-2"><section className="rounded-2xl border bg-white p-5 shadow-sm sm:p-6"><h2 className="font-semibold text-slate-950">Questions answered by bank</h2><p className="mt-1 text-xs text-slate-500">Answer volume across used question banks</p><div className="mt-5 h-72" aria-label="Questions answered by bank chart"><ResponsiveContainer width="100%" height="100%"><BarChart data={[...byBank]} margin={{ left: -20, right: 8 }}><CartesianGrid vertical={false} stroke="#d9e0e2" /><XAxis dataKey="name" axisLine={false} tickLine={false} tick={{ fontSize: 11, fill: "#64748b" }} /><YAxis axisLine={false} tickLine={false} tick={{ fontSize: 11, fill: "#64748b" }} /><Tooltip /><Bar dataKey="answered" fill="#52798a" radius={[6, 6, 0, 0]} /></BarChart></ResponsiveContainer></div></section><section className="rounded-2xl border bg-white p-5 shadow-sm sm:p-6"><h2 className="font-semibold text-slate-950">Answer accuracy</h2><p className="mt-1 text-xs text-slate-500">Correct versus incorrect answers</p><div className="mt-5 h-72" aria-label="Correct versus incorrect answers chart"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={answerResult} dataKey="value" nameKey="name" innerRadius={65} outerRadius={95} paddingAngle={3}>{answerResult.map((entry) => <Cell key={entry.name} fill={entry.color} />)}</Pie><Tooltip /><Legend /></PieChart></ResponsiveContainer></div></section></div>;
}
